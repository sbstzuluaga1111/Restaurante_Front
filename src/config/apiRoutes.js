// config/apiRoutes.js
const API_BASE_URL = "http://localhost:3010";

const apiRoutes = {
    menu: `${API_BASE_URL}/menu`,
    // Agrega aquí más rutas en el futuro si es necesario
};

export default apiRoutes;
